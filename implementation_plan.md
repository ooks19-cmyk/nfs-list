# 맛집 리스트 공유 웹 애플리케이션 구현 계획

네이버 지도 리스트 링크를 공유하고, 누구나 자유롭게 추천/댓글을 남길 수 있는 커뮤니티형 맛집 지도 공유 웹 애플리케이션을 구축합니다. GitHub Pages에 빌드 없이 바로 배포할 수 있는 순수 웹 표준(HTML5 + 모던 Vanilla JS + CSS3)과 Firebase Firestore를 기반으로 합니다.

---

## 1. 주요 기능 및 요구사항

1. **맛집 리스트 등록 & 네이버 지도 링크 연동**
   - 제목, 지역 태그(예: 구미, 서울, 제주 등), 한줄 설명, 네이버 지도 리스트 링크 URL 입력
   - 글 작성 시 **4자리 이상 비밀번호** 설정 (SHA-256 해시 암호화 후 안전하게 저장)
   - 네이버 지도 링크 형식 검증 및 1-클릭 네이버 지도 앱/웹 바로가기 버튼

2. **수정 및 삭제 권한 관리**
   - 카드 내 [수정] / [삭제] 버튼 클릭 시 비밀번호 확인 모달 표시
   - 비밀번호 일치 시에만 내용 수정 또는 삭제 수행

3. **좋아요 (추천) 시스템**
   - 접속자 누구나 원클릭 좋아요 및 좋아요 취소 가능
   - `localStorage` 기반 중복 방지 + Firestore 카운트 원자적 업데이트

4. **익명 댓글 시스템**
   - 위트 있는 랜덤 닉네임 자동 생성 (예: *‘군침 도는 미식가 #42’*, *‘바삭한 탕수육러버’* 등) 및 사용자 직접 수정 가능
   - 실시간 댓글 등록 및 타임스탬프 표시
   - 댓글 작성 시 비밀번호 옵션으로 개별 댓글 삭제 지원

5. **탐색, 검색 및 정렬**
   - 지역 태그 빠른 필터 칩 (전체, 서울, 부산, 대구, 구미, 제주 등 자주 찾는 지역)
   - 통합 검색바 (제목, 설명, 지역명 실시간 필터링)
   - 정렬 옵션 (최신순 / 좋아요 많은 순 / 댓글 많은 순)

6. **Firebase Firestore 연동 + 오프라인/데모 모드 지원**
   - `firebase-config.js`에 설정값만 넣으면 즉시 실시간 클라우드 DB 연동
   - Firebase 키 입력 전에도 즉시 로컬에서 동작을 확인할 수 있는 LocalStorage 데모 모드 내장

---

## 2. 사용자 디자인 & 스타일 가이드 (UX/UI)

- **테마 컬러**: 웜 오렌지 & 코랄 악센트 (`#FF5722`, `#FF7A45`, `#FFF7F2`)
- **타이포그래피**: Pretendard & Toss Product Sans 감성의 깔끔한 모던 폰트
- **인터랙션**: 부드러운 모달 트랜지션, 반응형 카드 그리드, 토스트 메시지 알림, 1-클릭 클립보드 복사
- **모바일 완벽 대응**: 스마트폰 네이버 지도 앱과 연동하기 쉽도록 모바일 뷰포트에 최적화된 반응형 UI

---

## 3. 프로젝트 파일 구조

```
gourmet list/
├── index.html            # 메인 웹 애플리케이션 진입점 및 모달/컴포넌트 템플릿
├── css/
│   └── style.css         # 웜 푸디 테마 디자인 시스템, 반응형 레이아웃, 모달/토스트 애니메이션
├── js/
│   ├── firebase-config.js # Firebase Firestore 연동 설정 (샘플 키 및 설정 가이드)
│   ├── store.js          # Firestore 및 LocalStorage 하이브리드 데이터 매니저 (CRUD, 좋아요, 댓글)
│   ├── nickname.js       # 재미있는 한국어 익명 닉네임 생성기
│   ├── security.js       # 비밀번호 SHA-256 해싱 및 유효성 검사 모듈
│   └── app.js            # UI 렌더링, 이벤트 핸들링, 검색/필터/정렬, 모달 제어
└── README.md             # 프로젝트 소개, GitHub Pages 배포 가이드 및 Firebase 설정 설명서
```

---

## 4. 제안 변경 사항

### 프론트엔드 UI & 구조
- [NEW] [index.html](file:///g:/내%20드라이브/투자정리2/gourmet%20list/index.html) : 헤더, 검색/필터바, 카드 리스트, 모달들 (등록/수정, 비밀번호 확인, 댓글 상세), 토스트
- [NEW] [css/style.css](file:///g:/내%20드라이브/투자정리2/gourmet%20list/css/style.css) : 웜 푸디 테마, 모던 카드 UI, 애니메이션, 모바일 최적화 반응형

### 로직 및 데이터베이스 매니저
- [NEW] [js/security.js](file:///g:/내%20드라이브/투자정리2/gourmet%20list/js/security.js) : Web Crypto API 기반 SHA-256 비밀번호 해싱
- [NEW] [js/nickname.js](file:///g:/내%20드라이브/투자정리2/gourmet%20list/js/nickname.js) : 한국어 랜덤 미식가 닉네임 생성기
- [NEW] [js/firebase-config.js](file:///g:/내%20드라이브/투자정리2/gourmet%20list/js/firebase-config.js) : Firebase Firestore SDK 연결 설정
- [NEW] [js/store.js](file:///g:/내%20드라이브/투자정리2/gourmet%20list/js/store.js) : Firestore + LocalStorage 하이브리드 CRUD 및 좋아요/댓글 제어
- [NEW] [js/app.js](file:///g:/내%20드라이브/투자정리2/gourmet%20list/js/app.js) : 이벤트 핸들러, 필터/정렬/검색 렌더링, 모달 관리

### 배포 및 사용 설명서
- [NEW] [README.md](file:///g:/내%20드라이브/투자정리2/gourmet%20list/README.md) : GitHub Pages 1분 배포 가이드 및 Firebase 설정 가이드

---

## 5. 검증 계획

1. **로컬 브라우저 기능 검증**
   - 초기 데모 맛집 데이터 자동 로딩 및 카드 렌더링 확인
   - [맛집 리스트 등록] 폼 입력 후 신규 카드 등록 확인
   - [네이버 지도 바로가기] 클릭 시 해당 링크 새 탭 오픈 확인
   - [좋아요] 원클릭 시 카운트 증가/감소 및 중복 방지 확인
   - [댓글 등록] 시 랜덤 닉네임 적용 및 실시간 댓글 렌더링 확인
   - [수정/삭제] 시 올바른 비밀번호 인증 모달 동작 확인
   - 지역 태그 칩 필터링 및 검색어 실시간 필터링 동작 확인
2. **GitHub Pages 배포 호환성 검증**
   - 모든 경로 상대경로(`.` 기준) 확인 및 빌드 불필요 여부 검증
